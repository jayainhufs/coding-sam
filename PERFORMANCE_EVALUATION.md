# Coding-Sam 성능 평가 가이드

## 개요

Coding-Sam의 성능을 정량적으로 측정하고 평가하기 위한 도구입니다.

## 측정 지표

### 1. API 응답 시간
- 각 엔드포인트별 평균, 최소, 최대, 중앙값
- 95th percentile (P95), 99th percentile (P99)
- 성공률

### 2. 페이지 로드 시간
- 전체 로드 시간
- DOMContentLoaded 시간
- First Paint (지원되는 경우)
- First Contentful Paint (지원되는 경우)

### 3. LocalStorage 성능
- 읽기 평균 시간 (μs)
- 쓰기 평균 시간 (μs)

## 사용 방법

### 방법 1: 웹 페이지 GUI 사용

1. 개발 서버 실행:
   ```bash
   npm run dev
   ```

2. 브라우저에서 접속:
   ```
   http://localhost:3000/performance
   ```

3. 설정:
   - API 호출 횟수 선택 (1-10회)
   - 측정할 엔드포인트 선택
   - "성능 평가 실행" 버튼 클릭

4. 결과 확인:
   - 화면에 결과 표시
   - 브라우저 콘솔에 상세 리포트 출력
   - JSON 다운로드 가능

### 방법 2: 브라우저 콘솔에서 실행

1. 개발 서버 실행 후 브라우저에서 사이트 접속

2. 개발자 도구(F12) 열기 → Console 탭

3. 다음 코드 실행:
   ```javascript
   // 성능 평가 유틸리티 import
   import('/utils/performance-evaluation.ts').then(({ 
     runPerformanceEvaluation, 
     printPerformanceReport 
   }) => {
     // 빠른 평가 (각 API 2회 호출)
     runPerformanceEvaluation(2).then(report => {
       printPerformanceReport(report)
     })
   })
   ```

4. 또는 더 간단하게:
   ```javascript
   // 전역 함수 사용 (public/performance-test.js 로드 후)
   runPerformanceTest(3)  // 각 API 3회 호출
   quickPerformanceTest()  // 빠른 평가 (2회)
   fullPerformanceTest()  // 전체 평가 (5회)
   ```

## 측정 대상 API 엔드포인트

- `/api/problems` - 문제 목록 조회 (GET)
- `/api/run` - 코드 실행
- `/api/ai/analyze-style` - 코드 스타일 분석
- `/api/ai/evaluate-style` - 스타일 평가
- `/api/ai/refactor` - 코드 리팩토링
- `/api/ai/feedback` - AI 피드백
- `/api/quiz/generate` - 퀴즈 생성

## 결과 해석

### API 응답 시간 기준

- **우수**: 평균 < 500ms
- **양호**: 평균 500-1000ms
- **보통**: 평균 1000-2000ms
- **개선 필요**: 평균 > 2000ms

### 성공률 기준

- **우수**: 99% 이상
- **양호**: 95-99%
- **보통**: 90-95%
- **개선 필요**: 90% 미만

### 페이지 로드 시간 기준

- **우수**: < 1초
- **양호**: 1-2초
- **보통**: 2-3초
- **개선 필요**: > 3초

## 주의사항

1. **API 속도 제한**: OpenAI API와 Piston API는 속도 제한이 있을 수 있습니다. 너무 많은 호출을 한 번에 하지 마세요.

2. **네트워크 환경**: 측정 결과는 네트워크 환경에 영향을 받습니다. 일관된 환경에서 측정하세요.

3. **캐싱**: 첫 호출은 캐시되지 않은 상태이므로 더 느릴 수 있습니다. 여러 번 측정하여 평균을 내는 것이 좋습니다.

4. **환경 변수**: OpenAI API 키가 설정되어 있어야 AI 관련 API가 정상 작동합니다.

## 리포트 저장

평가 결과를 JSON 파일로 저장하여 나중에 비교 분석할 수 있습니다:

```javascript
// 브라우저 콘솔에서
savePerformanceReport()
```

또는 웹 페이지에서 "JSON 다운로드" 버튼 클릭

## 성능 개선 팁

1. **느린 API 식별**: 리포트에서 가장 느린 엔드포인트를 확인하고 최적화
2. **캐싱 전략**: 자주 호출되는 API에 캐싱 적용
3. **병렬 처리**: 여러 API를 동시에 호출할 수 있는 경우 Promise.all 사용
4. **로딩 최적화**: 페이지 로드 시간이 느리면 코드 스플리팅, 이미지 최적화 등 고려

## 예시 리포트

```
📊 Coding-Sam 성능 평가 리포트
================================================================================
평가 시각: 2024-01-15 오후 3:30:00

📈 요약
--------------------------------------------------------------------------------
총 API 호출: 12회
실패 횟수: 0회
전체 성공률: 100%
평균 응답 시간: 856.5ms
가장 느린 엔드포인트: /api/ai/analyze-style
가장 빠른 엔드포인트: /api/problems

🔌 API 엔드포인트별 상세 메트릭
--------------------------------------------------------------------------------
POST /api/ai/analyze-style
  호출 횟수: 3회
  성공률: 100.00%
  평균 응답 시간: 1234.5ms
  최소: 1100.2ms
  최대: 1456.8ms
  중앙값: 1200.3ms
  P95: 1420.1ms
  P99: 1445.2ms
...
```

## 문제 해결

### API 호출 실패
- OpenAI API 키가 올바르게 설정되었는지 확인
- 네트워크 연결 확인
- API 속도 제한 확인

### 측정 결과가 비정상적으로 느림
- 네트워크 환경 확인
- 서버 리소스 확인
- 다른 프로세스가 리소스를 사용하고 있는지 확인

